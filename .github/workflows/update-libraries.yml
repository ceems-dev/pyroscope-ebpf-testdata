name: Update Libraries

on:
  schedule:
    - cron: "5 4 * * 1"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-libraries:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0  # v7.0.0
        with:
          persist-credentials: 'false'

      - name: Set up Docker
        uses: docker/setup-docker-action@v5
        with:
          daemon-config: |
            {
              "debug": true,
              "features": {
                "containerd-snapshotter": true
              }
            }

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4

      - name: Check and build Python libraries
        run: ./build_python.sh -i -p 4

      - name: Check and build MUSL libraries
        run: ./build_musl.sh -p 4

      - name: Check and build glibc libraries
        run: bash -x ./build_glibc.sh -p 2 -a amd64

      - name: Verify Changed files
        id: verify-changed-files
        run: |
          git status --short
          # If there are new files exit code will be 0 and 1 if there are no new files
          if git status --short | egrep -e '^\?(.*)$' > /dev/null; then
              echo "files_changed=true" >> $GITHUB_OUTPUT
          else
              echo "files_changed=false" >> $GITHUB_OUTPUT
          fi

      - name: Commit changes
        if: steps.verify-changed-files.outputs.files_changed == 'true'
        run: |
          git config --local user.email "bot@ceems.com"
          git config --local user.name "CEEMS Bot"
          git add -A
          git commit -m "chore: Update Python/MUSL/GLIBC libraries" -s

      - name: Push changes
        if: steps.verify-changed-files.outputs.files_changed == 'true'
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: ci/scheduled-workflow
